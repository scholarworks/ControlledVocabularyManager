<!DOCTYPE html>
<html lang="en">
  <head>
    <title><% if content_for?(:title) %><%= yield :title %> - <% end %>csunamespace</title>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= render :partial => 'shared/analytics' %>
    <%= csrf_meta_tags unless @skip_render %>
  </head>

  <body>
    <%= render :partial => 'shared/navbar' %>
    <div class="container-full">
      <div class="container">
        <div class="starter-template">
          <% flash.each do |key, value| %>
            <%= content_tag :div, value, class: "flash #{key} alert-#{key} alert" %>
          <% end %>
          <%= yield %>
        </div>
      </div><!-- /.container -->
    </div>
    <div class="footer">
      <div class="container">
        CsuNamespace is a linked data service, jointly maintained by by the <a href="http://libraries.calstate.edu/">California State University, Office of the Chancellor - Systemwide Digital Library Services</a>
    </div>
    </div>

    <%= render :partial => 'shared/ga_pageview' %>
  </body>
</html>
